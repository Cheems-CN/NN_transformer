# 三种融合策略对比

## 概述

针对脑肿瘤MRI影像分类任务，本项目实现了三种不同的CNN与Transformer融合策略，每种策略都有其独特的架构设计和优势。

## 1. 串行融合 (Hybrid Model) - 原有模型

### 架构特点
- CNN作为特征提取前端
- Transformer作为全局建模后端
- 特征按顺序依次处理

### 流程图
```
Input [224×224×3]
        ↓
   CNN提取器
[卷积层×4] → [14×14×512]
        ↓
    展平为序列
   [196×512]
        ↓
   投影+位置编码
   [197×512] (含CLS)
        ↓
  Transformer编码器
   [6层, 8头]
        ↓
   提取CLS token
      [512]
        ↓
    分类头
   [4类别]
```

### 优缺点
**优点**:
- 设计直观，易于理解
- 利用CNN提取的特征作为Transformer输入
- 参数量适中（~25M）

**缺点**:
- 串行结构可能导致特征信息损失
- CNN和Transformer之间缺乏直接的特征交互
- 实验结果显示性能不如Pure CNN

**适用场景**:
- 需要保持特征提取的层次性
- 希望明确区分局部和全局处理阶段

---

## 2. 并行融合 (Parallel Model) - 新增

### 架构特点
- CNN和Transformer同时处理输入
- 双分支独立提取特征
- 特征在后期融合

### 流程图
```
              Input [224×224×3]
                      |
        +-------------+-------------+
        ↓                           ↓
    CNN分支                     ViT分支
        |                           |
   [卷积层×4]              [Patch Embedding]
   [全局池化]              [Transformer×6]
        ↓                           ↓
     [512]                       [512]
        |                           |
        +-------------+-------------+
                      ↓
              特征拼接 [1024]
                      ↓
               融合层 (MLP)
              [1024→512→256]
                      ↓
                  分类头
                   [4类别]
```

### 优缺点
**优点**:
- 同时获取局部和全局特征
- 特征互补性强，信息更丰富
- 两个分支可以学习不同的表征
- 预期性能最优

**缺点**:
- 参数量最大（~31M）
- 计算成本高（需要同时运行两个分支）
- 可能需要更多训练数据防止过拟合

**适用场景**:
- 对准确率要求高的场景
- 计算资源充足
- 需要多尺度、多视角的特征表示

---

## 3. 嵌入式融合 (Embedded Model) - 新增

### 架构特点
- 在CNN主干中嵌入Transformer模块
- 采用MobileViT设计理念
- 轻量高效

### 流程图
```
Input [224×224×3]
        ↓
   Stem卷积
   [32通道]
        ↓
  Stage 1 (纯卷积)
   [64通道]
        ↓
  Stage 2 (纯卷积)
   [128通道]
        ↓
  Stage 3 (MobileViT块)
   [192通道]
    卷积 → Transformer → 卷积
        ↓
  Stage 4 (MobileViT块)
   [256通道]
    卷积 → Transformer → 卷积
        ↓
   全局平均池化
      [256]
        ↓
    分类头
   [4类别]
```

### MobileViT块详细结构
```
输入特征图 [H×W×C]
        ↓
   局部卷积 (3×3)
        ↓
   展平为序列 [HW×D]
        ↓
  Transformer块 (×2)
   [自注意力+FFN]
        ↓
   重组为特征图 [H×W×D]
        ↓
   投影卷积 (1×1)
        ↓
    残差连接
        ↓
输出特征图 [H×W×C']
```

### 优缺点
**优点**:
- 参数量最小（~4.4M），最轻量
- 计算效率高，适合移动端部署
- 在保持轻量的同时获得全局建模能力
- 渐进式设计，前期纯卷积，后期加入Transformer

**缺点**:
- 相比其他模型可能性能略低
- Transformer层数较少（每个块2层）
- 特征图尺寸变化需要仔细设计

**适用场景**:
- 资源受限环境（移动设备、边缘设备）
- 对推理速度有要求
- 需要在效率和性能间取得平衡

---

## 三种策略对比表

| 特性 | 串行融合 (Hybrid) | 并行融合 (Parallel) | 嵌入式融合 (Embedded) |
|------|------------------|--------------------|-----------------------|
| **参数量** | ~25M | ~31M | ~4.4M |
| **融合位置** | 特征提取后 | 分类前 | 网络中间层 |
| **计算复杂度** | 中等 | 高 | 低 |
| **特征互补性** | 低 | 高 | 中 |
| **训练难度** | 中等 | 较难 | 较易 |
| **推理速度** | 中等 | 慢 | 快 |
| **适合场景** | 层次化特征提取 | 高精度需求 | 移动端部署 |
| **预期性能** | 中等 | 最高 | 中等偏上 |
| **灵活性** | 一般 | 高（两分支独立） | 中（模块化设计） |
| **可解释性** | 高（流程清晰） | 中（双分支） | 中（多阶段） |

## 设计思路对比

### 串行融合
**核心思想**: "先局部后全局"
- CNN负责提取低级视觉特征（边缘、纹理）
- Transformer负责建模高级语义关系
- 类似于人类视觉：先关注细节，再理解整体

### 并行融合
**核心思想**: "同时多视角"
- CNN和ViT从不同角度理解图像
- CNN关注局部模式，ViT关注全局结构
- 两者特征互补，融合后信息更全面

### 嵌入式融合
**核心思想**: "局部全局交织"
- 在CNN处理过程中适时引入全局建模
- 保持CNN的效率优势
- 在关键位置加入Transformer增强能力
- 类似于在高速公路（CNN）中设置观景台（Transformer）

## 实验建议

### 训练顺序建议
1. 先训练轻量的Embedded模型，快速验证数据和流程
2. 再训练Hybrid模型，作为基线对比
3. 最后训练Parallel模型，追求最高性能

### 超参数调优
- **Parallel**: 可能需要更大的batch size和更长的训练时间
- **Embedded**: 可以尝试不同的base_channels和transformer_channels配比
- **所有模型**: 学习率和weight decay需要仔细调节

### 性能分析
观察以下指标的差异：
1. **训练时间**: Embedded < Hybrid < Parallel
2. **内存占用**: Embedded < Hybrid < Parallel
3. **收敛速度**: 可能 Embedded > Hybrid > Parallel
4. **最终准确率**: 预期 Parallel > Embedded ≈ Hybrid

## 总结

三种融合策略各有优劣：
- **Hybrid (串行)**: 适合理解基本的融合思路，但实验表明性能不佳
- **Parallel (并行)**: 追求最高性能，适合精度要求高的场景
- **Embedded (嵌入)**: 平衡效率和性能，适合实际部署

选择哪种模型取决于具体需求：
- 如果追求最高准确率 → **Parallel**
- 如果需要部署到移动端 → **Embedded**
- 如果想理解融合机制 → **Hybrid**

通过对比实验，可以深入理解不同融合策略对模型性能的影响，为未来的模型设计提供参考。
